# AppConnectReown - Wallet Authentication for ApeChain

แอปพลิเคชัน Flutter สำหรับเชื่อมต่อ Crypto Wallet (เช่น MetaMask, Trust Wallet) และใช้งาน ApeChain Network

## ฟีเจอร์หลัก

- 🔗 **เชื่อมต่อ Wallet** - รองรับ MetaMask, Trust Wallet และ wallet อื่นๆ ผ่าน WalletConnect
- 🔐 **ยืนยันตัวตน SIWE** - Sign-In with Ethereum (ไม่ต้องสร้างรหัสผ่าน)
- 💰 **ดูยอดเงิน** - แสดง balance จาก ApeChain blockchain
- 📤 **โอนเงิน (Send)** - โอน APE token ไปยัง address อื่นๆ
- 📥 **รับเงิน (Receive)** - แสดง QR code และ address สำหรับรับเงิน
- 🛒 **ซื้อเงิน (Buy)** - เชื่อมต่อไปยัง ApeChain Bridge
- 📋 **Copy Address** - คัดลอก wallet address ได้ง่าย
- 🚪 **ออกจากระบบ** - ตัดการเชื่อมต่อได้ตัวเองทุกเมื่อ
- 🎨 **UI สวยงาม** - ออกแบบ modern และใช้งานง่าย

## เทคโนโลยีที่ใช้

### Core Packages
- **[Flutter](https://flutter.dev)** - Framework สำหรับพัฒนาแอปข้ามแพลตฟอร์ม
- **[Reown AppKit](https://docs.reown.com)** (เดิมชื่อ WalletConnect) - เชื่อมต่อกับ Crypto Wallets
- **[web3dart](https://pub.dev/packages/web3dart)** - ติดต่อกับ blockchain
- **[SharedPreferences](https://pub.dev/packages/shared_preferences)** - จัดเก็บข้อมูลในเครื่อง
- **[QR Flutter](https://pub.dev/packages/qr_flutter)** - สร้าง QR code
- **[URL Launcher](https://pub.dev/packages/url_launcher)** - เปิด link ภายนอก
- **[http](https://pub.dev/packages/http)** - HTTP client สำหรับเชื่อมต่อ RPC
- **[intl](https://pub.dev/packages/intl)** - จัดการรูปแบบวันที่และตัวเลข

## ความต้องการระบบ

**สำหรับการพัฒนา:**
- Flutter SDK เวอร์ชัน 3.9.2 ขึ้นไป ([วิธีติดตั้ง](https://docs.flutter.dev/get-started/install))
- สำหรับ iOS: macOS + Xcode 14 ขึ้นไป
- สำหรับ Android: Android Studio

**สำหรับการใช้งาน:**
- มี Crypto Wallet ที่รองรับ WalletConnect (MetaMask, Trust Wallet, Rainbow, etc.)
- มี APE token บน ApeChain Network (สำหรับทดสอบการโอน)

## วิธีติดตั้งและรัน

### 1. Clone โปรเจค
```bash
git clone <repository-url>
cd flutter_rbh_reown
```

### 2. ติดตั้ง Dependencies
```bash
flutter pub get
```

### 3. ตั้งค่า Project ID (สำคัญ)

แอปต้องใช้ Reown (WalletConnect) สำหรับเชื่อมต่อ wallet ซึ่งต้องมี Project ID

**ใช้ ID ตัวอย่าง:** `ca221e0cb470bd7c95a9e849d71c340b`

**หรือสร้าง ID ของคุณเอง (แนะนำสำหรับ production):**
1. สมัครที่ [Reown Cloud](https://cloud.reown.com)
2. สร้าง Project ใหม่
3. Copy Project ID
4. แก้ไขที่ `lib/main.dart` บรรทัดที่ 54:
```dart
projectId: 'YOUR_PROJECT_ID_HERE',
```

### 4. ตั้งค่า Deep Link

#### iOS (ไฟล์: `ios/Runner/Info.plist`)
เพิ่มโค้ด:
```xml
<key>CFBundleURLTypes</key>
<array>
    <dict>
        <key>CFBundleURLSchemes</key>
        <array>
            <string>flutterrbhprivy</string>
        </array>
    </dict>
</array>
```

#### Android (ไฟล์: `android/app/src/main/AndroidManifest.xml`)
เพิ่มใน `<activity>` tag:
```xml
<intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="flutterrbhprivy" />
</intent-filter>
```

### 5. รันแอป

**iOS:**
```bash
flutter run -d ios
```

**Android:**
```bash
flutter run -d android
```

**ดูรายการอุปกรณ์ที่เชื่อมต่อ:**
```bash
flutter devices
```

### 6. Build สำหรับ Production

**iOS:**
```bash
flutter build ios --release
```

**Android APK:**
```bash
flutter build apk --release
```

**Android App Bundle (สำหรับ Play Store):**
```bash
flutter build appbundle --release
```

## วิธีใช้งานแอป

### ขั้นตอนการใช้งานครั้งแรก

1. **เปิดแอป**
   - จะเห็นปุ่มใหญ่ "Connect wallet"

2. **กดปุ่ม "Connect wallet"**
   - จะเปิด modal แสดงรายการ wallet ให้เลือก
   - เลือก wallet ที่คุณมี (เช่น MetaMask, Trust Wallet)

3. **อนุมัติการเชื่อมต่อใน Wallet**
   - แอป wallet จะถามให้อนุมัติการเชื่อมต่อ
   - กดปุ่ม "Connect" หรือ "อนุมัติ"

4. **Sign ข้อความ (SIWE)**
   - แอปจะขอให้ sign ข้อความเพื่อยืนยันตัวตน
   - เปิดแอป wallet และกด "Sign" (ไม่มีค่าใช้จ่าย ไม่ใช้ gas)
   - หลังจาก sign สำเร็จแอปจะจดจำการเข้าสู่ระบบ

5. **เริ่มใช้งาน**
   - เห็นหน้า Dashboard แสดง:
     - Wallet address
     - ยอดเงิน (balance) จาก blockchain
     - ปุ่ม Send, Receive, Buy

### ฟีเจอร์ต่างๆ ของแอป

#### 💰 ดูยอดเงิน (Balance)
- แอปจะดึงยอดเงินจาก ApeChain RPC
- แสดงเป็น APE token
- อัปเดตอัตโนมัติเมื่อเชื่อมต่อและหลังการโอน

#### 📤 โอนเงิน (Send)
1. กดปุ่ม "Send"
2. ใส่ Recipient Address (รองรับการสแกน)
3. ใส่ Amount (จำนวน APE)
4. กดปุ่ม "Send Transaction"
5. อนุมัติ transaction ในแอป wallet
6. รอ transaction สำเร็จ (balance จะอัปเดตอัตโนมัติ)

#### 📥 รับเงิน (Receive)
1. กดปุ่ม "Receive"
2. จะเห็น:
   - QR code ของ wallet address
   - Wallet address เป็น text (เพื่อ copy ได้)
3. แชร์ QR code หรือ address ให้ผู้ส่ง

#### 🛒 ซื้อเงิน (Buy)
1. กดปุ่ม "Buy"
2. เปิด browser ไปที่ ApeChain Bridge
3. Bridge token จาก network อื่นมาที่ ApeChain

#### 📋 Copy Address
- กดที่ Wallet Address Card
- Address จะถูก copy ไปที่ clipboard
- แสดงข้อความยืนยัน "Address copied to clipboard!"

#### 🚪 Disconnect
- กดปุ่ม "Disconnect Wallet" ด้านล่าง
- ตัดการเชื่อมต่อและหลังจากนั้นลบข้อมูลการเข้าสู่ระบบ

### การเข้าสู่ระบบครั้งถัดไป
- **ไม่ต้อง Sign ซ้ำ!** แอปจะจดจำว่าคุณ sign แล้ว
- เพียงแค่เชื่อมต่อ wallet ใหม่ก็เข้าใช้งานได้เลย

## 🔧 โครงสร้างโค้ด

```
lib/main.dart (ไฟล์เดียว ประมาณ 1,400+ บรรทัด)
│
├── MyApp                           # Root widget
└── WalletPage                      # หน้าหลัก
    └── _WalletPageState           # State management
        │
        ├── State Variables:
        │   ├── _appKitModal            # Reown AppKit instance
        │   ├── _web3client             # Web3 client สำหรับดู balance
        │   ├── _balance                # ยอดเงิน
        │   ├── _signature              # หลังจาก SIWE
        │   └── _isLoadingBalance       # Loading state
        │
        ├── Initialization:
        │   ├── _initializeReownAppKit() # ตั้งค่า WalletConnect
        │   ├── _initWeb3Client()        # ตั้งค่า Web3 client
        │   └── _onModalUpdate()         # จัดการการเชื่อมต่อ/ตัดการเชื่อมต่อ
        │
        ├── Wallet Functions:
        │   ├── _connectWallet()         # เชื่อมต่อ wallet
        │   ├── _disconnectWallet()      # ตัดการเชื่อมต่อ
        │   └── _getWalletAddress()      # ดึง address จาก session
        │
        ├── SIWE Functions:
        │   ├── _generateSIWEMessage()   # สร้างข้อความ SIWE
        │   ├── _signMessage()           # ขอ sign ข้อความ
        │   ├── _saveSignature()         # เก็บหลังจาก sign
        │   ├── _checkIfAlreadySigned()  # เช็คว่า sign แล้วหรือยัง
        │   └── _autoSignOnConnect()     # Auto-sign หลังเชื่อมต่อ
        │
        ├── Blockchain Functions:
        │   └── _fetchBalance()          # ดึงยอดเงินจาก blockchain
        │
        ├── UI Dialogs:
        │   ├── _showSendDialog()        # Dialog โอนเงิน
        │   └── _showReceiveDialog()     # Dialog รับเงิน (QR code)
        │
        └── UI Builders:
            ├── _buildDisconnectedView() # หน้ายังไม่เชื่อมต่อ
            ├── _buildConnectedView()    # หน้าเชื่อมต่อแล้ว
            └── _buildActionButton()     # ปุ่ม Send/Receive/Buy
```

## 🔐 SIWE (Sign-In with Ethereum) คืออะไร?

**SIWE** = วิธี login ใหม่ที่ใช้ลายเซ็นดิจิทัล แทนการใช้ username/password

### ทำไมถึงดี?
- ✅ ไม่ต้องจำรหัสผ่าน
- ✅ หลังจาก sign แล้วไม่ต้องส่งข้อมูลไปยัง server
- ✅ ปลอดภัยและควบคุมได้ด้วยตัวเอง
- ✅ ไม่มีค่าใช้จ่าย gas (เพราะไม่ใช่ transaction บน blockchain)
- ✅ มาตรฐาน [EIP-4361](https://eips.ethereum.org/EIPS/eip-4361)

### ตัวอย่างข้อความ SIWE:
```
appconnectreown.com wants you to sign in with your Ethereum account:
0x1234...5678

Welcome to AppConnectReown! Click "Sign" to authenticate.

URI: https://appconnectreown.com
Version: 1
Chain ID: 33139
Nonce: 1234567890123
Issued At: 2024-01-01T00:00:00Z
```

## 🌐 ApeChain Network

**Chain ID:** 33139
**RPC URL:** https://rpc.apechain.com/http
**Native Token:** APE
**Explorer:** https://apechain.calderaexplorer.xyz

## ❓ แก้ไขปัญหาที่พบบ่อย

### ปัญหา: แอปไม่เห็นหน้า connect wallet
**วิธีแก้:** ตรวจสอบว่าได้ตั้งค่า Deep Link ใน `Info.plist` (iOS) และ `AndroidManifest.xml` (Android) แล้ว

### ปัญหา: Balance แสดง 0.00 แม้จะมีเงิน
**วิธีแก้:**
- ตรวจสอบว่า wallet มี APE token บน ApeChain Network
- ตรวจสอบว่า RPC URL (`https://rpc.apechain.com/http`) ใช้งานได้
- Pull to refresh หรือตัดการเชื่อมต่อและเชื่อมต่อใหม่

### ปัญหา: โอนเงินไม่ได้
**วิธีแก้:**
- ตรวจสอบว่ามี balance เพียงพอ
- ตรวจสอบว่า recipient address ถูกต้อง (ต้องขึ้นต้นด้วย 0x และยาว 42 ตัวอักษร)
- ตรวจสอบว่า wallet app อนุมัติ transaction แล้ว

### ปัญหา: Build ไม่ผ่าน
**วิธีแก้:**
```bash
flutter clean
flutter pub get
flutter pub run flutter_launcher_icons
flutter build <platform>
```

## 📚 เอกสารสำหรับการพัฒนา

### ฟีเจอร์ที่แนะนำเพิ่มเติม

1. **เพิ่ม Network อื่นๆ**
   - แก้ไข `optionalNamespaces` ใน `_initializeReownAppKit()`
   - เพิ่ม chain ID ที่ต้องการ

2. **เพิ่ม Token อื่นๆ**
   - ใช้ `web3dart` อ่าน token balance (ERC-20)
   - เพิ่ม token contract address

3. **Transaction History**
   - เรียกใช้ ApeChain Explorer API
   - แสดงรายการ transactions ย้อนหลัง

### API References
- [Reown AppKit Docs](https://docs.reown.com)
- [web3dart Documentation](https://pub.dev/documentation/web3dart/latest/)
- [ApeChain Docs](https://apechain.com)

## 🏆 Credits

- Reown (WalletConnect) - Wallet connectivity
- Flutter Team - Cross-platform framework
- ApeChain - Blockchain network

---

Made with Flutter 💙
