# AppConnectReown - แอป Wallet ง่ายๆ

แอปกระเป๋าเงินดิจิทัลที่ทำด้วย Flutter สามารถเชื่อมต่อกับกระเป๋าเงิน Crypto ของคุณ (เช่น MetaMask, Trust Wallet) ได้ง่ายๆ แล้วใช้ลายเซ็นอิเล็กทรอนิกส์เพื่อยืนยันตัวตน

## 💡 แอปนี้ทำอะไรได้บ้าง?

- 🔐 **เชื่อมต่อกระเป๋าเงิน** - เชื่อมต่อกับ MetaMask, Trust Wallet หรือ wallet อื่นๆ ได้
- ✍️ **ยืนยันตัวตน** - ใช้ลายเซ็นดิจิทัลแบบ SIWE (ปลอดภัย ไม่ต้องใช้รหัสผ่าน)
- 💾 **จำคุณได้** - ไม่ต้อง sign in ใหม่ทุกครั้ง แอปจะจำคุณไว้
- 🎨 **หน้าตาสวย** - UI ออกแบบสไตล์ modern พร้อมสีสันสวยงาม
- 🔗 **รองรับ ApeChain** - ใช้งานบน ApeChain network ได้เลย
- 📱 **ใช้ได้ทั้ง iOS และ Android** - สร้างแอปครั้งเดียว รันได้ทั้ง 2 เครื่อง

## 📋 ต้องเตรียมอะไรบ้าง?

**สำหรับนักพัฒนา:**
- ติดตั้ง Flutter เวอร์ชัน 3.9.2 ขึ้นไป ([วิธีติดตั้ง Flutter](https://docs.flutter.dev/get-started/install))
- สำหรับทำแอป iOS: ต้องมี Mac + Xcode 14 ขึ้นไป
- สำหรับทำแอป Android: ติดตั้ง Android Studio

**สำหรับผู้ใช้งาน:**
- มีกระเป๋าเงินดิจิทัล เช่น MetaMask, Trust Wallet หรือ Rainbow Wallet
- กระเป๋าต้องรองรับ WalletConnect (wallet ส่วนใหญ่รองรับอยู่แล้ว)

## 🚀 ติดตั้งแอปยังไง?

### ขั้นตอนที่ 1: ดาวน์โหลดโค้ด

```bash
git clone <repository-url>
cd flutter_rbh_reown
```

### ขั้นตอนที่ 2: ติดตั้ง packages ที่จำเป็น

```bash
flutter pub get
```

คำสั่งนี้จะติดตั้ง library ต่างๆ ที่แอปต้องใช้ (เหมือนกับการติดตั้ง plugins)

### ขั้นตอนที่ 3: ตั้งค่าไอคอนแอป (ไม่บังคับ)

```bash
flutter pub run flutter_launcher_icons
```

คำสั่งนี้จะสร้างไอคอนแอปให้อัตโนมัติ

## 🔧 การตั้งค่า (สำคัญ!)

### เรื่อง Project ID

แอปนี้ต้องใช้ Project ID จาก Reown เพื่อเชื่อมต่อกับ wallet (เหมือน API key)

**ตอนนี้ใช้ ID ทดสอบ:** `ca221e0cb470bd7c95a9e849d71c340b` (อยู่ในไฟล์ `lib/main.dart` บรรทัดที่ 54)

**ถ้าอยากได้ ID ของตัวเอง (ฟรี):**
1. ไปสมัครที่ [Reown Cloud](https://cloud.reown.com)
2. สร้าง Project ใหม่
3. Copy Project ID ที่ได้
4. เปิดไฟล์ `lib/main.dart` แล้วแก้ไขที่บรรทัด 54

**ข้อมูลเพิ่มเติม:**
- รองรับ **ApeChain Network** (Chain ID: 33139)
- ใช้ **Deep Link**: `flutterrbhprivy://` (สำหรับกลับมาแอปหลัง connect wallet)

### ตั้งค่า iOS (ถ้าจะใช้บน iPhone/iPad)

ต้องเพิ่ม URL scheme เพื่อให้แอปเปิดกลับมาได้หลังจาก connect wallet

**เปิดไฟล์:** `ios/Runner/Info.plist`

**เพิ่มโค้ดนี้เข้าไป:**
```xml
<key>CFBundleURLTypes</key>
<array>
    <dict>
        <key>CFBundleURLSchemes</key>
        <array>
            <string>flutterrbhprivy</string>
        </array>
    </dict>
</array>
```

### ตั้งค่า Android (ถ้าจะใช้บน Android)

เหมือนกัน ต้องเพิ่ม intent filter

**เปิดไฟล์:** `android/app/src/main/AndroidManifest.xml`

**หา `<activity>` tag แล้วเพิ่มโค้ดนี้ข้างใน:**
```xml
<intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="flutterrbhprivy" />
</intent-filter>
```

> **หมายเหตุ:** ถ้าไม่เพิ่มโค้ดพวกนี้ แอปจะไม่สามารถเปิดกลับมาหลังจาก connect wallet ได้

## ▶️ รันแอปยังไง?

### รันแบบทดสอบ (Development)

**สำหรับ iPhone/iPad:**
```bash
flutter run -d ios
```

**สำหรับ Android:**
```bash
flutter run -d android
```

> **Tips:** ถ้ามีหลายเครื่อง ใช้ `flutter devices` เพื่อดูเครื่องที่เชื่อมต่ออยู่

### สร้างแอปเวอร์ชันเผยแพร่ (Production)

**สำหรับ iPhone/iPad:**
```bash
flutter build ios --release
```

**สำหรับ Android:**
```bash
# สร้างไฟล์ APK (ติดตั้งได้เลย)
flutter build apk --release

# หรือสร้างไฟล์ AAB (อัพโหลด Play Store)
flutter build appbundle --release
```

## 🎯 วิธีใช้งานแอป (สำหรับผู้ใช้)

### ครั้งแรกที่ใช้งาน

1. **เปิดแอป**
   - จะเห็นหน้าจอให้กดปุ่ม "Connect wallet"

2. **กดปุ่ม "Connect wallet"**
   - จะมีรายการ wallet ให้เลือก เช่น MetaMask, Trust Wallet
   - เลือก wallet ที่คุณใช้อยู่

3. **ไปที่แอป Wallet เพื่ออนุมัติ**
   - แอป wallet จะเด้งขึ้นมาขออนุมัติ
   - กดปุ่ม "Connect" หรือ "อนุมัติ"

4. **Sign ข้อความเพื่อยืนยันตัวตน**
   - กลับมาที่แอป จะขอให้ Sign ข้อความ (ฟรี ไม่เสียค่าธรรมเนียม)
   - เปิดแอป wallet อีกครั้งแล้วกด "Sign"

5. **เสร็จสิ้น!**
   - จะเข้าสู่หน้า Dashboard
   - แสดง wallet address, ยอดเงิน และลายเซ็นที่เซฟไว้

### ครั้งต่อไปที่เปิดแอป

- **ไม่ต้อง Sign ใหม่!** แอปจะจำลายเซ็นของคุณไว้แล้ว
- เปิดแอปแล้วใช้งานได้เลย

## 🛠️ เทคโนโลยีที่ใช้สร้างแอป

แอปนี้ใช้ library (packages) หลักๆ ดังนี้:

- **[Flutter](https://flutter.dev)** - ตัวสร้างแอปข้ามแพลตฟอร์ม (iOS + Android)
- **[Reown AppKit](https://docs.reown.com)** - ตัวเชื่อมต่อกับ Crypto Wallet (WalletConnect)
- **[SharedPreferences](https://pub.dev/packages/shared_preferences)** - เก็บข้อมูลไว้ในเครื่อง (เหมือน LocalStorage)
- **[URL Launcher](https://pub.dev/packages/url_launcher)** - เปิด link ภายนอกและจัดการ Deep Links
- **[Intl](https://pub.dev/packages/intl)** - จัดรูปแบบวันที่และเวลา

## 📁 โครงสร้างโค้ด (สำหรับนักพัฒนา)

โค้ดทั้งหมดอยู่ในไฟล์เดียว: `lib/main.dart`

```
lib/main.dart
├── MyApp                           # หน้าจอหลักของแอป
├── WalletPage                      # หน้า Wallet Page
│
└── ฟังก์ชันสำคัญๆ:
    ├── _initializeReownAppKit()    # เริ่มต้น Reown AppKit
    ├── _connectWallet()            # เชื่อมต่อ wallet
    ├── _disconnectWallet()         # ตัดการเชื่อมต่อ
    ├── _signMessage()              # ขอลายเซ็นจาก wallet
    ├── _generateSIWEMessage()      # สร้างข้อความ SIWE
    ├── _saveSignature()            # บันทึกลายเซ็น
    └── _checkIfAlreadySigned()     # เช็คว่าเคย sign แล้วหรือยัง
```

## 🔐 SIWE คืออะไร? (Sign-In with Ethereum)

**SIWE** = การ login ด้วยลายเซ็นดิจิทัล แทนการใช้ username/password

**ทำไมถึงปลอดภัย?**
- ไม่ต้องส่งรหัสผ่านไปที่ server
- ลายเซ็นพิสูจน์ได้ว่าคุณเป็นเจ้าของ wallet จริงๆ
- ฟรี! ไม่เสียค่าธรรมเนียม (เพราะไม่ใช่ transaction)

**รูปแบบข้อความที่จะให้คุณ Sign:**
```
appconnectreown.com wants you to sign in with your Ethereum account:
0x1234...5678

Welcome to AppConnectReown! Click "Sign" to authenticate.

URI: https://appconnectreown.com
Version: 1
Chain ID: 33139
Nonce: [เลขสุ่มไม่ซ้ำ]
Issued At: [เวลาที่สร้าง]
```

## 📝 แผนพัฒนาฟีเจอร์ (Roadmap)

**ทำเสร็จแล้ว ✅:**
- [x] เชื่อมต่อ/ตัดการเชื่อมต่อ Wallet
- [x] ยืนยันตัวตนด้วย SIWE
- [x] จำ session ไว้ (ไม่ต้อง login ใหม่ทุกครั้ง)
- [x] แสดงข้อมูล Wallet

**กำลังจะทำ 🚧:**
- [ ] ส่ง Token/Crypto
- [ ] รับ Token (แสดง QR Code)
- [ ] ซื้อ Crypto
- [ ] ดูประวัติ Transactions
- [ ] รองรับหลาย Chain (ไม่ใช่แค่ ApeChain)

## 🐛 ปัญหาที่รู้อยู่ (Known Issues)

**ฟีเจอร์ที่ยังไม่ทำงาน:**
- ยอดเงิน (Balance) แสดงเป็น 0.00 ETH เสมอ (ยังไม่ได้ดึงข้อมูลจริง)
- ปุ่ม Copy address กดแล้วไม่ copy (ต้องเพิ่ม clipboard package)
- ปุ่ม Send, Receive, Buy กดแล้วแค่แสดงข้อความ "Coming soon"

**ถ้าเจอปัญหา:**
- ลองปิดแอปแล้วเปิดใหม่
- ตรวจสอบว่าได้ตั้งค่า Deep Link ครบแล้ว (iOS/Android)
- ดู log ใน console (ใช้ `flutter run` ใน terminal)

## 🤝 อยากช่วยพัฒนาแอป?

ยินดีต้อนรับทุกคนที่อยากร่วมพัฒนา! ทำได้ง่ายๆ:

1. **Fork** โปรเจกต์นี้ไปที่ GitHub ของคุณ
2. **Clone** มาที่เครื่องของคุณ
3. **สร้าง branch ใหม่** สำหรับฟีเจอร์ของคุณ
   ```bash
   git checkout -b feature/ฟีเจอร์ใหม่ของฉัน
   ```
4. **แก้ไขโค้ด** และ test ให้เรียบร้อย
5. **Commit & Push**
   ```bash
   git commit -m 'เพิ่มฟีเจอร์ใหม่ที่เจ๋งๆ'
   git push origin feature/ฟีเจอร์ใหม่ของฉัน
   ```
6. **เปิด Pull Request** ใน GitHub

**ไอเดียฟีเจอร์ที่ต้องการ:**
- ดึงยอดเงิน (Balance) จริงจาก Blockchain
- เพิ่มฟีเจอร์ส่ง/รับ Token
- รองรับหลาย Blockchain
- Dark mode
- ภาษาอังกฤษ

## 📄 License

โปรเจกต์นี้สร้างขึ้นเพื่อการศึกษาและเป็นตัวอย่าง (Educational & Demo purposes)

สามารถนำไปใช้ แก้ไข และเผยแพร่ต่อได้อย่างอิสระ

## 📞 ติดต่อ & รับความช่วยเหลือ

**เจอปัญหา? มีคำถาม?**

- 🐛 เจอ bug → เปิด [Issue](../../issues) ใน GitHub
- 💡 มีไอเดีย → เปิด [Discussion](../../discussions)
- 📧 ติดต่อโดยตรง → สร้าง issue แล้วแท็ก maintainer

**ช่วยเหลือได้ตลอด 24/7** (พยายามตอบภายใน 1-2 วัน)

---

Made with ❤️ using Flutter & Reown AppKit

**สนุกกับการพัฒนาแอป!** 🚀
